"""Execute domain: holds authority, moves money.

Stub in M1 — its only job today is to prove the boundary exists from commit 1
(import-linter forbids any recommend <-> execute import). The gate, single-use
token verification, RBAC, and Sepolia tx construction arrive in slice C (M2).
"""
